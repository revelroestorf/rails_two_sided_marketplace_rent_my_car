<p id="notice"><%= notice %></p>

</div>

<div class="">
  <%= image_tag "#{@car.model.downcase}.jpeg", style: "background-size: cover; width: 100vw;" %>
</div>

<div class="mx-4 my-2">
  <div class="row d-flex justify-content-around">

    <div class="">
        <span class="text-muted"><%= "#{@car.user.firstname.downcase.capitalize}'s" %></span>
        <br>
        <span class="h2"><%= "#{@car.make.downcase.capitalize} #{@car.model.downcase.capitalize}" %></span>
        <span class="text-muted"><small><%= @car.year %></small></span>
    </div>

    <div class="car-deets mx-2 mt-4">
      <table class="table table-bordered">
        <tbody>
          <tr>
            <td>Location:</td>
            <td><%= @car.full_address %></td>
          </tr>
          <tr>
            <td>Price per day:</td>
            <td><%= number_to_currency(@car.price_per_day) %></td>
          </tr>
          <tr>
            <td>Price per km:</td>
            <td><%= number_to_currency(@car.price_per_km) %></td>
          </tr>
        </tbody>
      </table>
    </div>

    <div class="mt-4">

      <!-- <% form_tag(bookings_index_path, :method => "get", class: "btn btn-primary ml-2", style: "width:90px;") do %>
        <%= label_tag(:days, "number of days:") %>
        <%= text_field_tag :days, class: "form-control" %>
        <%= submit_tag("Book") %>
      <% end %>

      <%= hidden_field_tag :car_id, @car.id %>

    </div> -->


      <input class="form-control form-inline mb-5" placeholder="number of days" style="max-width:188px;">
      <div class="form-inline">
        <span><%= link_to 'go back', cars_path, class: "btn btn-dark", style: "color: white; width:90px;" %></span>
        <span><%= button_to 'Book', bookings_index_path, method: :get, params: {car_id: @car.id}, class: "btn btn-primary ml-2", style: "width:90px;" %></span>
      </div>
    </div>

    <!-- <button type="submit" class="ml-2 btn btn-primary">Book</button> -->

  </div>

  <div class="row">
  </div>

</div>

<br><br>
